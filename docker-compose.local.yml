services:
  exporter:
    image: mail-exporter:local
    build:
      context: .
      dockerfile: ./exporter/Dockerfile
      target: development
    environment:
      MAIL_ACCOUNTS_JSON: ${MAIL_ACCOUNTS_JSON:-[]}
    volumes:
      - ./exporter:/app
      - ./output:/output
